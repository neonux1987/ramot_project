import React from 'react';
import SlashModeButton from './SlashModeButton';
import { Edit } from '@material-ui/icons';

const EditButton = props => <SlashModeButton {...props} Icon={Edit} />

export default EditButton;